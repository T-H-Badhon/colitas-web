import AppBody from '@/components/terms/AppBody';
import HeroSection from '@/components/terms/HeroSection';
import React from 'react';

const Apppage = () => {
    return (
        <div>
            <HeroSection/>
            <AppBody/>
        </div>
    );
};

export default Apppage;